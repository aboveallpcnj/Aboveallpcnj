{
  "name": "above-all-pest-control",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Above All Pest Control static website with an optional local development preview.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "vite",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=22.13.0" },
  "devDependencies": { "vite": "8.0.13" }
}
