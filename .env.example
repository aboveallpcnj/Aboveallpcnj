# Set these as server-side Netlify environment variables, never in public files.
# Leave disabled until Google billing, quotas and the correct listing are confirmed.
GOOGLE_REVIEWS_ENABLED=false
GOOGLE_PLACES_API_KEY=
GOOGLE_PLACE_ID=
