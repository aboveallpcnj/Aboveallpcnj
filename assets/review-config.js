// Alex supplied the SociableKIT embed in index.html; keep its public ID there.
export const reviewConfig = Object.freeze({
  sociableKitEnabled: true,
  // Set true only after confirming the saved widget rating filter in SociableKIT.
  minimumRatingConfirmed: false,
  // Optional legacy integration; Google Places supplies at most five reviews.
  placesSampleEnabled: false,
});
